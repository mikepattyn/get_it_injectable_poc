name: get_it_injectable_poc
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  get_it: ^7.6.0
  injectable:
    path: ./dependencies/injectable/injectable

  get_it_poc:
    path: ./packages/get_it_poc
  shared:
    path: ./packages/shared
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  injectable_generator: 
    path: ./dependencies/injectable/injectable_generator
  build_runner: ^2.1.8

flutter:
  uses-material-design: true
